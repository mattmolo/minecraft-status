[project]
name = "minecraft-status"
version = "0.1.0"
description = "Web server for displaying players online a Minecraft server with a basic API"
readme = "README.md"
requires-python = ">=3.10"
dependencies = ["flask>=3.1.3", "gunicorn>=25.1.0", "mcstatus>=12.2.1"]
